{
	"extends": "./tsconfig.shared.json",
	"include": ["ghscripts/**/*.ts"],
	"compilerOptions": {
		"module": "NodeNext",
		"moduleResolution": "NodeNext",
		// match output dir to input dir. e.g. dist/index instead of dist/src/index
		"rootDir": "ghscripts",
		"outDir": "dist",
		// "isolatedModules": true,
		"emitDeclarationOnly": false
	}
}
